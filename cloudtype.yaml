version: "2.0"
services:
  maptic:
    name: maptic
    type: docker
    context: ./
    dockerfile: Dockerfile
    port: 8080
    env:
      - MONGODB_URI
      - JWT_SECRET
      - NAVER_SEARCH_CLIENT_ID
      - NAVER_SEARCH_CLIENT_SECRET
      - NODE_ENV=production
